{
  "name": "my-app",
  "scripts": {
    "clean": "npx rimraf dist & npx rimraf **/generated",
    "serve:gulp": "npx gulp generate & npx gulp live",
    "serve:eleventy": "npx @11ty/eleventy --watch --serve --output=dist --input=src",
    "serve": "npx npm-run-all clean --parallel serve:*",
    "deploy": "deploy.sh"
  },
  "dependencies": {
    "axios": "^0.19.2",
    "chart.js": "^2.9.3",
    "json-formatter-js": "^2.3.4",
    "tippy.js": "^6.2.3"
  },
  "devDependencies": {
    "browserify": "^16.5.1",
    "eleventy": "^1.0.6",
    "gulp": "^4.0.2",
    "gulp-imagemin": "^7.1.0",
    "gulp-livereload": "^4.0.2",
    "gulp-sass": "^4.1.0",
    "gulp-uglify": "^3.0.2",
    "rimraf": "^3.0.2",
    "uglify-es": "^3.3.9"
  }
}
